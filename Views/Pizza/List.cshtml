@model PizzaListViewModels

<div>
    <h2>@Model.CurrentCategory</h2>
</div>
<div class="container">
    <div class="row gap-3">
        @foreach(var pizza in Model.Pizzas){
    <partial name="_PizzaSummary" for="@pizza"/>
}
    </div>
</div>