@using AloPizza.Models
@model Pizza

  <div class="card col-sm-4 col-md-3 col-lg-4" style="width: 18rem;">
    <h4>@Model.Name</h4>
  <img class="card-img-top h-100" src="@Model.ImageUrl" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">Preço: @Model.Price</h5>
    <a asp-controller="Pizza" asp-action="Details" asp-route-pizzaId="@Model.PizzaId">@Model.Name</a>

    <p>@Model.ShortDescription</p>
     <div>
        <p class="button">
            <a class="btn btn-primary" id="cartButton" asp-controller="ShoppingCart"
            asp-action="AddItemToShoppingCart" asp-route-PizzaId="@Model.PizzaId">
            Incluir no Carrinho
            </a>
        </p>
    </div>
  </div>
</div>